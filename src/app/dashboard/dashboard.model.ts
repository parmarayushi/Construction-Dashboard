/** Device Chart type */
export type Tasks = {
  label: string;
  value: number;
  color: string;
};

export type Costs = {
  label: string;
  value: number;
  color: string;
};

export type WorkLoad = {
  label: string;
  value: number;
  color: string;
};
